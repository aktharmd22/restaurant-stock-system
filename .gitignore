/.phpunit.cache
/node_modules

# public/build is deliberately NOT ignored. Hostinger shared hosting has no
# Node, so the compiled CSS and JS have to travel in the repository - without
# them a `git pull` on the server serves an app with no styling at all.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
